{
  "name": "@pantry/registry-app",
  "version": "0.10.23",
  "type": "module",
  "description": "Pantry registry test application using ts-cloud",
  "scripts": {
    "test:s3": "bun run test-s3.ts",
    "test:bucket": "bun run test-s3.ts",
    "test:dynamodb": "bun run test-dynamodb.ts",
    "pack": "bun run pack.ts",
    "publish": "bun run publish.ts",
    "lookup": "bun run lookup.ts"
  },
  "dependencies": {
    "@stacksjs/ts-cloud": "^0.7.26"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
