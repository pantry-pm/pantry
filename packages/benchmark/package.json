{
  "name": "@pantry/benchmark",
  "type": "module",
  "version": "0.10.47",
  "private": true,
  "description": "Performance benchmarks for pantry",
  "scripts": {
    "benchmark": "bun run pm-benchmark.ts",
    "benchmark:small": "bun run pm-benchmark.ts --fixture=small",
    "benchmark:medium": "bun run pm-benchmark.ts --fixture=medium",
    "benchmark:large": "bun run pm-benchmark.ts --fixture=large",
    "benchmark:cold": "bun run pm-benchmark.ts --scenario=cold",
    "benchmark:warm": "bun run pm-benchmark.ts --scenario=warm",
    "benchmark:composer": "bun run pm-benchmark.ts --managers=pantry,composer",
    "benchmark:vs-composer": "bun run pantry-vs-composer.ts",
    "file-detection": "bun run file-detection-comparison.ts",
    "build": "echo no-op"
  },
  "devDependencies": {
    "mitata": "^1.0.34"
  }
}
